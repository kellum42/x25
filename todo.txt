- Multiple select on items page and get metrics on selected (like SUM, MAX, etc)
- List times when balance dips below a threshold value
- Give dollar figure of how much (per week and or month?) they can limit expenses or increase income to not go under
- Replace breadcrumbs with segmented index. Maybe have tabs for Forecasting, Past (accounting), Verifications
- On simulations have chart with new budget overlayed on parent budget chart
- Change useBudgetDetails -> useBudget
- Make local storage util responsible for just interacting with local storage. It should do no other logic.
- Change verifyAmount to verifyBudgetItem in useBudgetDetails.ts
- Error handling on UpdateBudget
- Populate BudgetCard with correct data.
- Error handling on index.tsx
- On delete budget item, confirmation popup doesnt go away after submit.
- Make datepicker start at populated date, if populated in "edit budget item".
- Notes widget on dashboard page. Be able to tag budget items (and maybe tag simulations).
- Add tags to budget items. (ex. "Out of Budget" for budget items that weren't accounted for)
- Add notes field to budget items
- Change "Edit Budget" button to "Add Item". Add "Edit Budget" to menu button next to title on dashboard page.
- List day item occurs for (bi)weekly items for "Upcoming" filter on items page.
- (Bi)Weekly items only occur once on "Upcoming" filter on items page.